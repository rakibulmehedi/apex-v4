global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: 'apex_v4'
    static_configs:
      - targets: ['host.docker.internal:8000']
