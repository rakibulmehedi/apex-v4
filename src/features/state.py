"""
src/features/state.py — Redis TTL state manager + PostgreSQL WAL writer.

Phase 1 (P1.5): Implementation pending.
Redis keys: fv:{pair} TTL 300s
All writes also go to PostgreSQL feature_vectors table.
"""
# TODO: Phase 1 — P1.5 implementation
