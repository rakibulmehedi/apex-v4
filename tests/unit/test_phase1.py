"""
tests/unit/test_phase1.py — Phase 1 exit gate.

Phase 1 (P1.6): Unit tests for all TA-Lib indicators.
Exit criteria: pytest tests/unit/test_phase1.py → ALL PASSED

Tests:
  - ATR vs known values
  - ADX vs known values
  - EMA200 vs known values
  - Bollinger Bands vs known values
  - FeatureVector populates correctly
  - Redis TTL keys set correctly (mocked Redis)
  - PostgreSQL WAL write called (mocked DB)
"""
# TODO: Phase 1 — P1.6 implementation
